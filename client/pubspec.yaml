# name: client
# description: "TN-Epic Flutter app."
# publish_to: 'none'

# version: 1.0.0+1

# environment:
#   sdk: ^3.12.2

# dependencies:
#   flutter:
#     sdk: flutter
#   google_fonts: ^6.2.1

# dev_dependencies:
#   flutter_test:
#     sdk: flutter

# flutter:
#   uses-material-design: true

# dependencies:
#   flutter:
#     sdk: flutter
#   camera: ^0.11.0

name: client
description: "TN-Epic Flutter app."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^8.1.0
  google_maps_flutter: ^2.6.0
  geolocator: ^14.0.3
  location: ^8.0.1
  camera: ^0.12.0+1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
